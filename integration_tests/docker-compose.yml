version: '3'

services:
  host1:
    build:
      context: ../packages/backend
      dockerfile: Dockerfile
    ports:
     - "4040:4000"
  host2:
    build:
      context: ../packages/backend
      dockerfile: Dockerfile
    ports:
     - "4041:4000"